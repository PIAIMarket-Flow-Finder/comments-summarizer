apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: comments-summarizer-stateful
  labels:
    app: comments-summarizer
spec:
  serviceName: comments-summarizer
  replicas: 1
  selector:
    matchLabels:
      app: comments-summarizer
  template:
    metadata:
      labels:
        app: comments-summarizer
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
      - name: comments-summarizer
        image: ghcr.io/piaimarket-flow-finder/list-vectorizer:latest
        # If you build the image locally, change the next line to `imagePullPolicy: Never` - there is no need to pull the image
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 80
        envFrom:
          - configMapRef:
              name: comments-summarizer-config
